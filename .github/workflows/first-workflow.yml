 name: First workflow1
 on:
    push:
        branches:
            -main
jobs:
    my job:
        runs-on: ubuntu-latest
        steps:
            -name: Print the word 
            run: :echo "Print the word"